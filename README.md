# react-native-recycle-basket

React-Native Recycle Basket game

![](game-recycle.gif)

### Installation

Install Yarn:
Open (https://yarnpkg.com/)

In the project folder run the terminal codes:

```
$ yarn install
```

```
$ react-native run-ios
```
